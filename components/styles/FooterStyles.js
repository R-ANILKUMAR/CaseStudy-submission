import styled from "styled-components";

const FooterStyles = styled.main`
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0 auto;
  width: 100vw;
  max-width: var(--max-width);
  height: 3.15rem;
  background-color: var(--light-grey);
  p {
    padding-bottom: 0;
    margin: auto 0;
  }
`;

export default FooterStyles;
